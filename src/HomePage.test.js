import { render, screen } from '@testing-library/react';
import Home from './components/Home';

test('renders learn react link', () => {
  // Write tests here... you will have to create routes
  // when rendering components, like in index.js
});
